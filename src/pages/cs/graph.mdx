---
layout: "../../layouts/MarkdownLayout.astro"
---
import BFS from "../../components/daa/graph/BFS.vue";

# Breadth First Search
Given a source vertex s, breadth-first search (BFS) finds **all** the
vertices reachable from s and, for each reachable vertex v it finds the
shortest-path length $\delta(s,v)$ and a shortest path from s to v.

<BFS client:visible />

# Depth First Search

--TODO--
